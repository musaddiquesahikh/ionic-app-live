import { CapitalizeInputDirective } from './capitalize-input.directive';

describe('CapitalizeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalizeInputDirective();
    expect(directive).toBeTruthy();
  });
});
